{
  "name": "notion-multi-mcp",
  "version": "0.1.0",
  "description": "Multi-account Notion MCP server hosted on Cloudflare Workers. Parity with the native Notion MCP, plus per-account OAuth so you can switch between client workspaces in one Claude session.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit",
    "test": "tsx test/roundtrip.ts && tsx test/ddl-roundtrip.ts && tsx test/view-roundtrip.ts && tsx test/duplicate-page.ts && tsx test/update-page-roundtrip.ts && tsx test/account-store.ts && tsx test/comments.ts && tsx test/bug-regressions.ts && tsx test/property-values.ts && tsx test/client-retry.ts && tsx test/oauth-tokens.ts && tsx test/api-surface.ts && tsx test/api-surface-2.ts && tsx test/block-write-schema.ts && tsx test/nested-containers.ts && tsx test/handler-wiring.ts && tsx test/validate-block-bodies.ts"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260728.1",
    "@notionhq/client": "^5.19.0",
    "tsx": "^4.21.0",
    "typescript": "^5.4.5",
    "wrangler": "^4.115.0"
  },
  "dependencies": {
    "marked": "^18.0.2"
  }
}
